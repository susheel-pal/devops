package com.example.iot_shorts

class DiffrentLEDPattern {

    //connect jumper wire on pin 1 and 6

    /*
    import RPi.GPIO as GPIO
    import time
    GPIO.setmode(GPIO.BOARD)
    GPIO.setup(7,GPIO.OUT)
    for i in range(10):
        GPIO.output(7,True)
        print("LED is finally on")
        time.sleep(1)
        GPIO.output(7,False)
        print("LED is off")
        time.sleep(1)
    print("program complete")
    GPIO.cleanup()
     */
}